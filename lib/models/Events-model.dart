class UserEvent {
  String? eventName;
  String? eventID;
  String? eventDate;
  String? eventStartTime;
  String? eventTicketUrl;
  String? eventPostcode;

  UserEvent(
      {this.eventName,
      this.eventID,
      this.eventDate,
      this.eventStartTime,
      this.eventTicketUrl,
      this.eventPostcode});
}
