name: musical
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  http: ^1.2.2
  webview_flutter_android: ^4.0.2
  webview_flutter_wkwebview: ^3.16.2

  webview_flutter: ^4.2.1

  cupertino_icons: ^1.0.8
  firebase_core: ^3.8.0
  flutter_calendar_carousel: ^2.4.4
  oauth2_client: ^3.0.0
  spotify_sdk: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  flutter_launcher_icons: "^0.14.1"

flutter:
  uses-material-design: true

  # assets:
  #   - /assets/images

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/musiCAL icon.jpeg"
